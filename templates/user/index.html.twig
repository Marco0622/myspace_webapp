{% extends 'base.html.twig' %}

{% block title %}Hello UserController!{% endblock %}

{% block body %}

<div class="d-flex align-items-center justify-content-between mb-4">
    <div>
        <h1 class="mb-0 fw-medium">Bonjour, {{ app.user.pseudo ?? (app.user.name ~ " " ~ app.user.firstname) }} </h1>
        <p class="text-white mb-0 ">Bienvenue sur votre espace ! {{ ux_icon('boxicons:smile-filled') }}</p>
    </div>
</div>

<div class="d-flex align-items-center justify-content-between w-100 py-2 border-bottom border-white pb-2">
    <h3 class="fw-medium mb-0">
        Invitations reçues
    </h3>
    <a class="btn btn-light d-flex align-items-center gap-2">
        {{ ux_icon('zondicons:reload') }} Actualiser
    </a>
</div>

{{ dump() }}

<div class="row g-3 my-2">
    {% for Invitation in arrInvitation %}
        <div class="col-12 mb-2">
            <div class="card border">
                <div class="card-body py-2">
                    <div class="row align-items-center g-3">
                        <div class="col-12 col-sm-auto text-center">
                            <span class="badge bg-success">Invitation</span>
                        </div>
                        <div class="col-12 col-md">
                            <h6 class="mb-0 fw-medium text-white text-center text-md-start text-uppercase">Session Deep Learning</h6>
                            <small class="text-white-50 d-block text-center text-md-start">Par : Jean Dupont • il y a 2h</small>
                        </div>
                        <div class="col-12 col-md-auto d-flex flex-column flex-md-row gap-2 ms-md-auto">
                            <button class="btn btn-sm btn-success d-flex align-items-center justify-content-center gap-1 w-100 w-md-auto">
                                {{ ux_icon('rivet-icons:check') }} Accepter
                            </button>
                            <button class="btn btn-sm btn-outline-danger d-flex align-items-center justify-content-center gap-1 w-100 w-md-auto">
                                {{ ux_icon('lucide:x') }} Refuser
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    {% else %}
        <h5> Aucun Invitation pour le moments  </h5>
    {% endfor %}
</div>

<div class="d-flex align-items-center justify-content-between w-100 py-2 border-bottom border-white pb-2">
    <h3 class="fw-medium mb-0">
        Mes sessions
    </h3>
    <a class="btn btn-light d-flex align-items-center gap-2">
        {{ ux_icon('fa7-solid:plus') }} Créer une session
    </a>
</div>

<div class="row g-3 py-3">
    {% for session in arrSession %}
        <div class="col-12 col-md-6 col-xl-4">
            <div class="card border h-100">
                <div class="card-body d-flex flex-column gap-2">
                    <div class="d-flex align-items-start justify-content-between">
                        <span class="badge bg-primary">Session</span>
                        <small class="text-white">10 avr. 2026</small>
                    </div>
                    <h6 class="mb-0 fw-medium text-white text-uppercase">Intro au Machine Learning</h6>
                    <div class="d-flex align-items-center gap-2 text-white">
                        owner
                    </div>
                    <div class="d-flex align-items-center gap-1 text-white small">
                        {{ ux_icon('tdesign:member-filled') }} 4 membres
                    </div>
                </div>
                <div class="card-footer border-top d-flex gap-2 py-2 px-3">
                    <a href="#" class="btn btn-sm btn-primary flex-fill">Rejoindre</a>
                    <button class="btn btn-sm btn-outline-danger">
                        {{ ux_icon('octicon:trash-24') }}
                    </button>
                </div>
            </div>
        </div>
    {% else %}
        <h5> Aucun Invitation pour le moments  </h5>
    {% endfor %}
</div>

{% endblock %}
