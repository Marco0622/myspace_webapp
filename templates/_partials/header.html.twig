<nav class="navbar navbar-expand-lg navbar-light mb-5">
    <div class="container">
        <a class="navbar-brand" href="/">
            <img src="{{ asset('assets/images/icon/logo.svg') }}" alt="MySpace logo" height="50">
        </a>

        <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMain" aria-controls="navbarMain" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarMain">
            <ul class="navbar-nav ms-auto align-items-center text-uppercase">
                {% if app.user %}
                    {% if 'ROLE_ADMIN' in app.user.roles %}
                        <li class="nav-item">
                            <a class="nav-link text-white fw-medium d-flex align-items-center" href="{{ path('app_user_dashboard') }}">
                                {{ ux_icon('bi:speedometer2', {class: 'me-2', width: '20px', height: '20px'}) }}
                                Dashboard
                            </a>
                        </li>
                    {% endif %}
                    <li class="nav-item dropdown ms-lg-3">
                        <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" id="profileDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <div class="rounded-circle overflow-hidden border border-2 border-white" style="width: 40px; height: 40px;">
                                {% if app.user.photo %}
                                    <img src="{{ asset('assets/uploads/user/' ~ app.user.photo) }}" alt="Profil" class="w-100 h-100 object-fit-cover">
                                {% else %}
                                    <img src="https://ui-avatars.com/api/?name={{ app.user.name }}&background=random" alt="Profil" class="w-100 h-100">
                                {% endif %}
                            </div>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end shadow border-0" aria-labelledby="profileDropdown">
                            <li><h6 class="dropdown-header text-capitalize pe-5">{{ app.user.firstname }} {{ app.user.name }}</h6></li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item d-flex align-items-center pe-5 text-white" href="{{ path('app_user_update', { id: app.user.id }) }}">
                                    {{ ux_icon('bi:person-circle', {class: 'me-2'}) }} Mon Profil
                                </a>
                            </li>
                            <li><h6 class="dropdown-header text-capitalize pe-5">Session</h6></li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item d-flex align-items-center text-danger pe-5" href="{{ path('app_logout') }}">
                                    {{ ux_icon('bi:box-arrow-right', {class: 'me-2'}) }} Déconnexion
                                </a>
                            </li>
                        </ul>
                    </li>

                {% else %}
                    <li class="nav-item">
                        <a class="nav-link text-white fw-medium" href="{{ path('app_login') }}">Connexion</a>
                    </li>
                    <li class="nav-item w-100 w-lg-auto mt-2 mt-lg-0">
                        <a class="nav-link fw-medium text-white ms-lg-3 px-4 rounded-pill border border-white" href="{{ path('app_register') }}">Inscription</a>
                    </li>
                {% endif %}

                <li class="nav-item w-100 w-lg-auto mt-2 mt-lg-0 d-flex align-items-center border-start ms-lg-3 ps-lg-3">
                    <a class="nav-link fw-medium text-white p-0" href="#">
                        {{ ux_icon('bi:question-circle-fill', {width: '25px', height: '25px'}) }}
                    </a>
                </li>
            </ul>
        </div>
    </div>
</nav>